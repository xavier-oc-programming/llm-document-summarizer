streamparse is a lightweight open-source Python library for building real-time data processing pipelines, designed for teams who need stream processing without the complexity of Flink or Spark. It provides a composable pipeline DSL, built-in windowing, exactly-once processing semantics via checkpointing, and a dead-letter queue for failed records. The library supports Kafka and Redis sources and exposes Prometheus metrics for pipeline observability.
